query OrderNonFulfillableLineItems($orderId: ID!, $first: Int, $after: String) {
  order(id: $orderId) {
    nonFulfillableLineItems(first: $first, after: $after) {
      nodes {
        id
      }
      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}
