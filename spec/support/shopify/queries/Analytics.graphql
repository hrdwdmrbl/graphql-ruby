query Analytics($shopifyQl: String!) {
  shopifyqlQuery(query: $shopifyQl) {
    tableData {
      columns {
        dataType
        displayName
        name
        subType
      }
      rows
    }
    parseErrors
  }
}